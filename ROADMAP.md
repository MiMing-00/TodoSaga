# TodoSaga 로드맵

> 상세 기획: [docs/ITEMS.md](docs/ITEMS.md) · 세계관: [docs/LORE.md](docs/LORE.md) · 디자인: [DESIGN.md](DESIGN.md)

## 지금 어디까지 왔나

| 영역 | 상태 |
|---|---|
| 디자인 시스템 (픽셀 + 양피지) | 완료 — `DESIGN.md`, Galmuri11 |
| 로컬 저장 (zustand persist) | 완료 |
| 하루 단위 퀘스트 · 연속 기록 | 완료 |
| 능력치 5종 · 픽셀 캐릭터 | 완료 |
| 직업 · 해금 · 전직 경제 | 완료 |
| 소지품 (골드 · 전리품) | 완료 |
| 아이템 50종 · 인벤토리 · 슬롯 | 완료 |
| 퀘스트 드랍 · 직업 풀 필터 | 완료 |
| 꽝 → 재료 → 조합 | 완료 |
| 장비 효과 실제 반영 | 완료 |
| 도구 → AI 프롬프트 주입 | 완료 |
| 하루 마감 · 디버프 발동 | 완료 |
| 화면 분할 (퀘스트 / 캐릭터 / 소지품 / 상점) | 완료 |
| 상점 (구매 · 판매) · 퀘스트 리롤 | 완료 |
| 기록 화면 「사가의 서」 | 완료 |
| 세계관 · 용어 정리 ([docs/LORE.md](docs/LORE.md)) | 완료 |
| 치장 스프라이트 25종 · 나비 픽셀 | 완료 |
| 앱 셸 (사이드바 / 하단 탭) | 완료 |
| 뽑기 「나비의 상자」 · 연출 | 완료 |
| 장비 강화 (성공/유지/파괴) · 파편 · 보호권 | 완료 |
| 상시 의뢰 (반복 루틴) | 완료 |
| 안내자 나비 = **고양이**로 확정 | 완료 |
| 연속 기록 보상 (7 / 30 / 100일) | 완료 |
| 완수 2단계 (한 줄 기록) · 완수 연출 | 완료 |
| 나비 산책 · 호감도 · 나비 꾸미기 (넓은 화면) | 완료 |

---

## 🔴 지금 당장 고쳐야 할 것

### ~~1. 장비와 도구가 거짓말을 하고 있다~~ → 완료

- 장비 효과가 EXP·골드·능력치 획득에 실제로 곱해진다. 카드에는 **실제로 받게 될 값**이 `▲`와 함께 표시된다
- 장착한 도구의 `prompt`가 `generateQuests`에 주입된다

> **작업 중 발견:** 도구 프롬프트만 넣어서는 효과가 화면에 안 나타났다.
> AI에게 "처음 5분에 할 일을 덧붙이라"고 지시해도 **퀘스트 JSON에 담을 필드가 없어서**
> 형식을 지키느라 정보를 버린 것이다.
> `first_step` / `estimated_minutes` / `tips` 선택 필드를 추가하고 프롬프트가 필드명을 직접
> 지목하게 고쳐서 해결했다. **지시만으로는 부족하고, 담을 그릇이 있어야 한다.**

### 1. 자정을 넘기면 화면이 안 바뀐다

페이지를 열어둔 채 날짜가 바뀌면 "오늘"이 갱신되지 않는다. 새로고침해야 한다.

---

## 새로 제안하는 것

### ~~A. 하루 마감~~ → 완료

브라우저 앱은 자정에 스스로 깨어날 수 없다. 그래서 **다음에 앱을 열었을 때**
지나간 날을 정산한다. 일주일을 비웠어도 정산 화면은 **가장 최근 하루만** 보여주고
나머지는 조용히 닫는다 — 모달을 일곱 번 띄울 수는 없다.

- 미완료 1개당 다음 날 EXP **-5%**, 최대 **-20%**. 골드와 연속 기록은 건드리지 않는다
- 퀘스트 완료 시 `earned`(실제 받은 양)를 기록해 둔다. 나중에 장비가 바뀌어도
  그날의 정산은 그대로여야 한다
- 디버프가 걸린 날은 카드에 `▼`와 함께 **깎인 값**이 표시된다

<details>
<summary>원래 기획 (참고)</summary>

### A. 하루 마감 — **debuff에 드디어 목적이 생긴다** ⭐

지금 이 앱에는 **시작만 있고 끝이 없다.** 퀘스트를 만들고 완료하지만, 하루가 닫히지 않는다.
그래서 AI가 생성한 `debuff`는 표시만 되고 아무 일도 일어나지 않는 장식이다.

> **하루가 끝날 때 정산 화면을 띄운다.**

```
┌─ 오늘의 사가가 저뭅니다 ────┐
│  완료 4 / 6                  │
│  얻은 것  +320 EXP  85 G     │
│  놓친 것  2개                │
│    · 영단어 30개 외우기       │
│      → 내일 몬스터가 조금     │
│        강해집니다             │
│  [ 내일도 이어간다 ]          │
└──────────────────────────────┘
```

- 미완료 퀘스트의 `debuff`가 여기서 **실제로 발동**한다 (가볍게: 다음 날 EXP 소폭 감소)
- **저녁에 앱을 열 이유**가 생긴다 — 루틴 앱은 아침보다 저녁 리텐션이 어렵다
- 하루를 닫는 행위 자체가 회고다

너무 아프면 앱을 안 열게 되므로 **"가볍게 놀리는" 수준**으로. 연속 기록은 건드리지 않는다.

</details>

### B. 기록 화면 (날짜별 회고) — 데이터는 이미 다 있다

`days`에 날짜별 퀘스트가 전부 쌓이는데 **최근 7일 도트로만 보인다.**
화면만 붙이면 되므로 투입 대비 효과가 크다.

- 월간 잔디 (능력치별 색으로)
- 날짜를 누르면 그날의 퀘스트
- "이번 달 가장 많이 한 것: INT 공부 12회"

### C. 반복 루틴 — 매일 같은 걸 입력하고 있다

루틴 앱인데 루틴을 저장할 수 없다.

- 자주 쓰는 퀘스트를 템플릿으로 저장 → 하루 시작 시 원클릭 소환
- AI 호출도 줄어든다 (비용·속도 둘 다 이득)

---

## 재미 점검 — 솔직한 평가

기능은 다 붙었다. 그런데 **"며칠 연속으로 할 만큼 재밌는가"**는 별개 문제다.
코드를 놓고 되짚어본 결과, 두 가지가 걸렸다.

### 1. 완료 버튼이 실제로 한 일과 연결돼 있지 않았다 ⚠

가장 근본적인 문제다. **누르기만 하면 보상이 나온다.**
5초면 오늘 몫이 전부 완수되고, 그걸 한 번 깨닫는 순간 경제 전체가 무의미해진다.
골드도 강화도 뽑기도 다 그 위에 서 있으므로 같이 무너진다.

> **손본 것:** 완수를 두 걸음으로 나눴다. 「완수」를 누르면
> **"무엇을 했나요?"** 한 줄을 적을 자리가 열린다. 「그냥 완수」로 건너뛸 수도 있다.
>
> 강제하지 않는 이유는, 강제하면 마찰이 커져 앱을 안 열게 되기 때문이다.
> 다만 **한 줄을 적는 게 기본 동선**이 되게 두면 자기기만이 조금 어려워지고,
> 그 한 줄이 「사가의 서」에 남아 회고가 된다. 숫자보다 문장이 오래 남는다.

### 2. 가장 자주 하는 행동에 손맛이 없었다

연출 시간을 세어보니 이랬다.

| 행동 | 빈도 | 연출 |
|---|---|---|
| 뽑기 | 가끔 | 1,250ms |
| 강화 | 가끔 | 800ms + 파편 |
| **완수** | **하루에 여러 번** | **0ms, 토스트 한 줄** |

명백한 불균형이다. 가장 자주 누르는 버튼이 제일 밋밋했다.

> **손본 것:** 완수 순간 카드가 흰색으로 번쩍이고
> **얻은 EXP·골드가 위로 떠오른다** (700ms).

### 아직 남은 약점

- ~~**하루 중 다시 열 이유가 없다.**~~ → 나비를 하루 5번 쓰다듬을 수 있게 해서
  일부 해소했다. 다만 알림 없이는 "열어야겠다"는 신호가 여전히 약하다
- **첫 세션이 비어 있다.** LV.1 견습생 · 빈 가방 · 빈 상점 · 빈 기록.
  나비가 누구인지, 자질이 왜 100이어야 하는지 설명이 없다 → **온보딩 필요**
- **AI 대기 7초.** "나비가 받아적는 중..."만 깜빡인다.
  나비가 실제로 뭔가 하는 연출이 있으면 체감이 줄어든다

### 그래서 재밌나

**도구(AI 프롬프트 모디파이어)가 이 앱의 진짜 무기다.**
「첫걸음의 신발」이 붙여주는 "처음 5분에 할 일"은 실제로 미루기를 줄인다.
게임 장치가 아니라 **여기가 사람에게 실제로 쓸모 있어지는 지점**이고,
다른 RPG 투두 앱과 갈라지는 부분이다.

반대로 말하면, 도구를 못 사면 이 앱은 그냥 예쁜 투두 앱이다.
도구는 400~1,500 G이고 하루 100 G쯤 버니 **4~15일치**다.
첫 도구를 손에 넣기까지가 이 앱의 진짜 튜토리얼인 셈인데,
그 구간이 지금 가장 비어 있다.

---

## 배포 전 체크리스트

개발 중에는 필요 없다. **실제 사용자의 데이터가 생기는 순간부터** 필요해지는 것들이다.

| 항목 | 왜 배포 전인가 |
|---|---|
| **persist 스키마 마이그레이션** | 지금은 `version: 1`에 `migrate` 함수가 없다. 필드를 추가할 때마다 기본값으로 때우고 있는데, 실제 데이터가 생긴 뒤 구조를 바꾸면 **남의 몇 달치 기록이 깨진다** |
| **AI 응답 검증** | `JSON.parse`를 통째로 try/catch 한다. 모델이 형식을 어기면 "오류가 발생했습니다"만 뜨고, 사용자가 할 수 있는 건 새로고침뿐이다. `category`/`rank` 보정 + 1회 재시도 |
| **JSON 내보내기/불러오기** | localStorage뿐이라 브라우저를 지우면 사가가 통째로 사라진다. 30분 |
| **환경변수 점검** | `GEMINI_API_KEY`, `ALLOWED_ID`, `QUEST_SYSTEM_PROMPT` 누락 시 동작 확인 |
| **`middleware` → `proxy` 이관** | 빌드마다 deprecated 경고가 뜬다 |

---

## 백엔드가 생긴 뒤에 할 것

지금 구조(localStorage + 단일 ID + DB 없음)로는 **만들 수 없거나, 만들어도 반쪽인** 것들이다.
Firebase를 붙이는 시점에 한 묶음으로 온다.

### 알림 (Web Push)

루틴 앱에 알림이 없는 건 분명한 약점이다. 다만 **지금 구조로는 불가능하다.**

- Web Push는 구독 정보(`PushSubscription`)를 **서버에 저장**하고,
  **서버가 정해진 시각에 VAPID 서명으로 발송**해야 동작한다 → DB + 스케줄러 필요
- 브라우저가 켜져 있을 때만 뜨는 알림은 만들 수 있지만 **무의미하다** —
  탭이 열려 있으면 이미 보고 있는 것이다
- `periodicBackgroundSync`는 크롬 전용이고, 주기를 브라우저가 정한다. "아침 8시"를 보장하지 못한다
- iOS는 홈 화면에 설치된 PWA만 Web Push를 받는다

**보낼 내용은 이미 정해져 있다** (백엔드가 생기면 바로):
- 아침: "오늘의 의뢰서를 작성할 시간입니다"
- 저녁: "퀘스트 2개가 남았어요" (하루 마감과 연결)
- **연속 기록이 끊기기 직전**: "오늘 하나만 완료하면 기록이 이어집니다" ← 가장 효과적

> **백엔드 없이 쓸 수 있는 대체재:** 반복 일정 `.ics` 파일 내보내기.
> 유저의 캘린더 앱이 대신 알려준다. 서버가 필요 없고 한 시간이면 된다.
> 알림만큼 좋지는 않지만, 아무것도 없는 것보다는 낫다.

### 기기 간 동기화 · 다중 사용자

- 지금은 브라우저 하나에 갇혀 있다
- 인증이 `ALLOWED_ID` 하나라 다중 사용자로 가려면 인증부터 갈아야 한다

---

## 있으면 좋겠지만 급하지 않은 것

- **캐릭터 카드 공유 이미지** — 스프라이트가 데이터라 서버에서 PNG로 그려내기 쉽다. 자랑할 거리
- **업적** — "일주일 개근", "한 분야 1000 달성". 연속 기록 보상과 묶으면 자연스럽다
- **퀘스트 재정렬 / 우선순위** — 지금은 생성 순서 고정
- **다크 모드** — 의도적으로 단일 테마를 골랐다. 필요해지면 그때
- **효과음** — 픽셀 UI에 잘 맞지만 스코프가 커진다

---

## 안 할 것

- 현금 결제 — 골드는 오직 하루를 산 대가로만 얻는다
- 소셜 피드 / 랭킹 — 남과 비교하는 순간 루틴이 남의 것이 된다
- 알림 남발 — 나중에 붙이더라도 하루 2회를 넘기지 않는다. 무시당하는 순간 끝이다

---

## 추천 순서

| 순서 | 항목 | 이유 |
|---|---|---|
| ~~0~~ | ~~장비 수치 · 도구 프롬프트~~ | **완료** |
| ~~0~~ | ~~하루 마감 · 상점 · 리롤 · 기록 화면~~ | **완료** |

| — | 알림 · 동기화 | 백엔드가 생긴 뒤 |

화면이 다섯으로 나뉘었고 세계관도 정리됐다.
**기획서 `docs/ITEMS.md`의 구현 항목이 전부 끝났다.**

남은 것은 성격이 다르다 — 화면을 붙이는 일이 아니라
**배포와 백엔드**다 ([배포 전 체크리스트](#배포-전-체크리스트),
[백엔드가 생긴 뒤에 할 것](#백엔드가-생긴-뒤에-할-것)).
