---
version: 0.1
name: TodoSaga-design-system
base: VoltAgent/awesome-design-md — notion (design/reference/notion-DESIGN.md)
description: "Notion의 따뜻한 종이 미니멀리즘 + 파스텔 태그 체계를 '픽셀화(pixelize)'한 시스템. 양피지 캔버스(#F4EEE0)에 딥 브라운 잉크(#2E2A24), 단일 브랜드 액센트 Saga Violet(#5A3FD6). 모든 radius는 0, 그림자는 blur 없는 하드 오프셋(4px 4px 0), 보더는 2~3px 솔리드 잉크. Notion이 데이터베이스 속성을 파스텔 칩으로 표현하듯, TodoSaga는 퀘스트의 카테고리(STR/INT/CHA/VIT/LUK)와 랭크(F~S)를 파스텔 칩으로 표현한다. 디스플레이/수치는 픽셀 폰트(Galmuri11), 본문은 Pretendard로 가독성을 지킨다."
---

## 왜 Notion을 골랐는가

Linear / Notion / Vercel 중 **Notion**.

| | 판단 |
|---|---|
| **Vercel** | 흑백 정밀 + Geist. 이미 `create-next-app` 기본값이라 "기본 템플릿 그대로"로 보인다. 아기자기함이 0에 가깝고 장식 시스템이 메시 그라디언트뿐이라 픽셀과 충돌. |
| **Linear** | `#010102` 니어블랙 + 라벤더 단일 액센트. RPG 상태창 느낌은 나지만 방향이 **차갑고 하드코어**(솔로레벨링 쪽)다. "아기자기 / 루틴을 지켜간다"와 반대. |
| **Notion** ✅ | 따뜻한 종이 캔버스(`#f6f5f4`), 브라운 잉크(`#37352f`), **파스텔 틴트 카드 6종**(peach/rose/mint/lavender/sky/yellow), 알약 아님·사각형 버튼. 셋 중 유일하게 "따뜻함 + 다색 태그 + 사각 기하학"을 다 가짐. |

결정적 이유 3가지:

1. **파스텔 태그 = 퀘스트 속성.** Notion의 파스텔 틴트는 원래 DB 속성 칩에서 온 것이다. TodoSaga의 STR/INT/CHA/VIT/LUK, F~S 랭크가 정확히 같은 구조 — 그대로 매핑된다.
2. **사각 기하학.** Notion은 "알약 버튼을 쓰지 않는다"가 명시된 규칙. 픽셀화할 때 radius만 `0`으로 내리면 되고, 구조를 안 부순다. Linear/Vercel은 라운드·그라디언트가 정체성이라 픽셀화하면 원본이 사라진다.
3. **종이 = 퀘스트 로그.** 따뜻한 오프화이트 캔버스는 양피지/퀘스트 두루마리로 확장하기 자연스럽다. 매일 채워가는 루틴 앱의 톤과 맞는다.

원본은 `design/reference/notion-DESIGN.md`에 저장됨.

---

## 픽셀화 변환 규칙 (Notion → TodoSaga)

이 4줄이 이 디자인 시스템의 전부다.

| 축 | Notion 원본 | TodoSaga |
|---|---|---|
| **Radius** | 버튼 8px / 카드 12px | **전부 `0`** (예외 없음. 픽셀은 곡선이 없다) |
| **Elevation** | `rgba(15,15,15,.08) 0 4px 12px` 확산 그림자 | **`4px 4px 0 {ink}`** — blur·spread 0의 하드 오프셋 |
| **Border** | `1px` 소프트 헤어라인 | **`3px solid {ink}`** (칩·인풋은 `2px`) |
| **Motion** | `cubic-bezier` 부드러운 이징 | **`steps(4, end)`** — 프레임이 끊겨야 픽셀이다 |

---

## Tokens

### Colors

```
/* Canvas & Ink — 양피지 */
--canvas:        #F4EEE0   /* 페이지 배경 (따뜻한 양피지) */
--surface:       #FFFBF0   /* 카드/패널 (밝은 종이) */
--surface-sunken:#E8DFCB   /* 인풋 안쪽, 비어있는 게이지 */
--ink:           #2E2A24   /* 본문/보더/하드섀도 (딥 브라운블랙) */
--ink-muted:     #6B6154   /* 보조 텍스트 */
--ink-disabled:  #A79B87

/* Brand */
--primary:       #5A3FD6   /* Saga Violet — 주 CTA 단 하나 */
--primary-press: #4530A8
--on-primary:    #FFFFFF

/* Semantic — 슬롯이 겹치지 않게 고정 */
--exp:           #2E7D32   /* EXP 게이지 */
--gold-fill:     #B45309   /* 골드 칩 배경 */
--gold-ink:      #7C2D12   /* 골드 텍스트 (칩 배경 위 대비 확보용) */
--danger:        #C0392B   /* 디버프 / 실패 */
--done:          #6B6154   /* 완료된 퀘스트 = 무채색으로 가라앉힘 */

/* Pastel tints — Notion 계보. 카테고리/랭크 칩 배경 */
--tint-peach:    #FFE8D4
--tint-rose:     #FDE0EC
--tint-mint:     #D9F3E1
--tint-lavender: #E6E0F5
--tint-sky:      #DCECFA
--tint-yellow:   #FEF3C7
--tint-gray:     #EDE7DA
```

**대비 검증 완료** (WCAG AA 4.5:1 기준, canvas `#F4EEE0` 위):
`ink` 12.33 · `ink-muted` 5.24 · `primary` 5.82 · `exp` 4.43 · `danger` 4.70.
`gold-fill`은 4.34로 미달 → **텍스트에는 `--gold-ink`를, 배경 채움에만 `--gold-fill`을** 쓸 것.

#### 카테고리 ↔ 틴트 매핑 (고정)

라벨은 **`QUEST_SYSTEM_PROMPT`가 AI에게 지시하는 분류와 반드시 일치**해야 한다. 프롬프트를 고치면 이 표도 같이 고칠 것.

| 카테고리 | 의미 | 틴트 | 잉크 |
|---|---|---|---|
| STR | 운동 | `--tint-rose` | `#9F1239` |
| INT | 공부/업무 | `--tint-sky` | `#1E40AF` |
| CHA | 사교 | `--tint-lavender` | `#5B21B6` |
| VIT | 건강 | `--tint-mint` | `#166534` |
| LUK | 취미 | `--tint-yellow` | `#854D0E` |

랭크 F~S는 **틴트 대신 보더 두께로** 표현한다 — 색을 더 쓰면 카테고리 칩과 싸운다.
F·D = `2px` 보더 / C·B = `3px` / A = `3px` + `--primary` 보더 / S = `3px` + `--primary` 채움 + 2프레임 깜빡임.

### Typography

```
--font-display: 'Galmuri11', monospace;   /* 제목, 레벨, 수치, 칩 — 한글 지원 픽셀 폰트 */
--font-body:    'Pretendard', system-ui;  /* 본문, objective, flavor_text */
```

> **왜 두 벌인가:** 픽셀 폰트로 본문까지 쓰면 AI가 생성한 긴 퀘스트 설명이 읽히지 않는다. 픽셀은 **정체성이 필요한 곳**(제목/숫자/칩)에만, 본문은 Pretendard로 읽히게 한다. Notion이 Notion-Sans 한 벌로 가는 것과 여기서 갈라진다.

| 토큰 | 폰트 | 크기 / 굵기 / 행간 |
|---|---|---|
| `display` | Galmuri11 | 32px / 400 / 1.3 — 앱 타이틀 |
| `level` | Galmuri11 | 24px / 400 / 1.2 — LV 표시, 골드 수치 |
| `card-title` | Galmuri11 | 18px / 400 / 1.4 — 퀘스트 제목 |
| `chip` | Galmuri11 | 12px / 400 / 1.0 — 랭크·카테고리·보상 칩 |
| `body` | Pretendard | 15px / 400 / 1.6 — objective |
| `body-sm` | Pretendard | 13px / 400 / 1.6 — flavor_text, debuff |

픽셀 폰트에는 `letter-spacing`을 주지 말 것. 비트맵 그리드가 깨진다.

### Spacing

8의 배수만 사용. 픽셀 그리드는 4/8px 단위로 스냅되어야 한다.
`4 · 8 · 16 · 24 · 32 · 48 · 64`

### Shape & Elevation

```
--radius: 0;                     /* 모든 곳 */
--border: 3px solid var(--ink);
--border-thin: 2px solid var(--ink);
--shadow: 4px 4px 0 0 var(--ink);        /* 카드/버튼 기본 */
--shadow-press: 2px 2px 0 0 var(--ink);  /* 눌림: translate(2px,2px) 동반 */
--shadow-lift: 6px 6px 0 0 var(--ink);   /* hover */
```

---

## Components

### `button-primary`
`--primary` 채움 / `--on-primary` 텍스트 / `--border` / `--shadow` / padding `12px 20px` / `display` 폰트.
누르면 `translate(2px, 2px)` + `--shadow-press`. 이 물리적 "눌림"이 픽셀 UI의 핵심 피드백이다.

### `quest-card`
`--surface` 채움 / `--border` / `--shadow` / padding `16px`.
완료 시: `--tint-gray` 배경 + 그림자 제거 + 텍스트 `--done` + `line-through`. **투명도(opacity)로 죽이지 말 것** — 픽셀 아트에서 반투명은 금기다.

### `chip`
카테고리 틴트 배경 / `--border-thin` / padding `4px 8px` / `chip` 타이포 / radius `0`.

### `exp-gauge`
높이 `16px` / `--surface-sunken` 트랙 / `--border-thin` / 채움 `--exp`.
**연속 애니메이션 금지.** `steps(10)`으로 10칸이 딱딱 차오르게 — 게이지가 눈금으로 읽혀야 한다.

### `stat-panel`
다섯 능력치의 누적 경험치. 막대는 **가장 높은 능력치 대비 상대값** — 절대 상한이 없으므로 "지금 어느 쪽으로 치우쳐 있는가"를 보여주는 게 목적이다. 막대 색은 카테고리 잉크색을 그대로 쓴다 (색 = 데이터 원칙).

### `streak-strip`
연속 기록 + 최근 7일 도트. 달성한 날은 `--primary` 채움, 미달성 과거는 `--sunken`, **오늘 미달성은 `--primary` 보더 + 깜빡임**. 루틴 앱의 핵심 지표라 퀘스트 목록보다 위에 둔다.

### 치장 스프라이트
25종을 하나하나 그리는 대신 **모양 13개 + 색 조합**으로 만든다
(`lib/cosmetics.ts`). 스프라이트가 데이터라 팔레트만 갈아끼우면 되므로 이게 훨씬 싸다.

- 슬롯당 팔레트 키를 다르게 매핑한다 (`k→1/2/3`, `j→4/5/6`) — 안 그러면 색이 서로 덮는다
- **등에 걸치는 것(망토·날개·배낭)은 몸보다 먼저** 그려야 뒤로 간다
- 망토는 몸(cols 3~12)보다 **확실히 넓고 길게**. 1~2px만 삐져나오면 옆구리 얼룩처럼 보인다

### `nabi-companion` — 넓은 화면에만
넓은 화면에는 내용 바깥으로 남는 공간이 크다. 그 여백을 나비가 걸어다닌다.
**좁은 화면에는 두지 않는다** — 여백이 없어 내용을 가리기만 한다.

- 위치를 **정수 픽셀**로만 옮기고, 걸음마다 몸이 1px 오르내린다.
  부드럽게 미끄러지면 픽셀이 아니게 된다
- 가끔 멈춰 앉고, **집사답게 책을 읽거나 비질을 한다**.
  졸기만 하면 배경에 죽은 고양이가 있는 꼴이다
- **옆모습은 귀를 하나만** 그린다. 둘을 다 그리면 정수리가 갈라져 지저분하다
- 귀 끝은 **1픽셀**이라야 뾰족해 보인다. 2픽셀이면 뭉툭한 뿔이 된다
- 꼬리는 걷기 두 프레임에서 각도를 달리해 **한 박자 늦게 따라오게** 한다.
  꼬리가 고정이면 몸통만 움직이는 인형처럼 보인다
- 일과(공부·청소)도 **두 프레임**이다. 책장이 넘어가고 빗자루가 기울어야 일하는 것으로 읽힌다
- 옆모습 머리는 **정면만큼 크게**, 바닥선도 맞춘다. 납작해지면 다른 동물처럼 보인다
- 대각선으로 미끄러지지 않는다. 한 높이에서 수평으로 걷고,
  높이를 바꿀 때만 **좌·우 방향이 있는 점프**로 건너뛴다
- `prefers-reduced-motion`이면 아예 움직이지 않는다
- **호감도 상한은 화면에 보여주지 않는다** (`17 / ???`, 진행 막대 없음).
  끝이 보이면 "만렙 찍었으니 그만"이 되는데, 이건 그런 종류의 관계가 아니다

### `pixel-sprite`
캐릭터는 **이미지가 아니라 데이터**다. 16×16 문자 그리드(`lib/sprite.ts`) + 팔레트를 SVG `<rect>`로 그린다. 외부 에셋이 없으니 능력치에 따라 팔레트를 갈아끼우는 게 공짜다.

- 가로로 연속된 같은 색은 하나의 `<rect>`로 합친다
- `shapeRendering="crispEdges"` 필수 — 없으면 확대할 때 가장자리가 뭉개진다
- 레이어는 아래→위 순서 (몸 → 장비)
- 그리드 문자: `.` 투명 / `o` 외곽선 / `s` 피부 / `h` 머리 / `c` 옷 / `d` 옷(그늘) / `a` 장비 강조
- 장비 강조색은 **옷 색과 충돌하지 않게** 고른다 (수도사 물약을 초록으로 두면 초록 로브에 묻힌다)

### `character`
직업은 유저가 직접 고르고, 능력치는 "고른 직업답게 살고 있는지"를 되비춘다.
칭호는 레벨 구간(견습/숙련/명예/전설)으로 붙인다 — 새 도트를 그리지 않고 성장을 보여주는 가장 싼 방법.

**노출 위계가 곧 유도 신호다.** 첫 전직만 카드에 버튼으로 두고, 재전직은
`캐릭터 정보 → 다른 길로 전향하기` 안쪽에 묻는다. 재전직 입구를 카드에 노출하면
앱이 "직업을 바꿔라"라고 부추기는 것처럼 읽히는데, 이 앱이 유도해야 하는 행동은
퀘스트를 쌓아 레벨을 올리는 것이다.

### `class-confirm`
되돌릴 수 없는 선택에만 쓰는 오버레이. 퀘스트 삭제는 인라인 2단계로 처리했지만
전직은 흐름을 끊는 게 맞다. 스크림은 blur가 아니라 **4px 체크무늬 디더**로 가린다.

### `vault`
모은 재화를 확인하는 곳. 골드는 크게, 전리품은 파스텔 칩으로. 전리품은 저장하지 않고 완료 기록에서 뽑아낸다.

### `tab-nav` · 화면 분할
한 페이지에 전부 쌓으면 스크롤만 길어지고 정작 **오늘 할 일이 화면 밖으로 밀린다.**
세 화면으로 나눈다.

| 라우트 | 내용 |
|---|---|
| `/` 퀘스트 | 디버프 알림 · 오늘의 기록 · 의뢰서 작성 · 퀘스트 목록 |
| `/character` 캐릭터 | 캐릭터(전직) · 연속 기록 · 능력치 |
| `/bag` 소지품 | 소지금·전리품 · 가방(슬롯/장비/치장/도구/재료) |

| `/shop` 상점 | 떠돌이 상단 (구매 · 판매) |
| `/chronicle` 기록 | 사가의 서 — 월간 잔디 · 날짜별 회고 |

#### 앱 셸 — 헤더가 없다

```
┌────────┬────────────────────┐
│ 로고   │                    │
│        │  main              │
│ 퀘스트 │  overflow-y-auto   │ ← 여기만 스크롤
│ 캐릭터 │                    │
│ ...    │                    │
│        │                    │
│ [상태] │                    │  ← 사이드바 하단
├────────┼────────────────────┤
│        │ [상태 스트립]       │  ← sm 미만
│        │ 하단 탭             │
└────────┴────────────────────┘
```

**화면 위쪽에 헤더를 매달지 않는다.** 레벨·EXP·골드는 길찾기에 붙인다 —
넓은 화면은 사이드바 하단, 좁은 화면은 하단 탭 바로 위 스트립.
위쪽은 내용에게 온전히 내준다.

- EXP는 **늘어나는 막대가 아니라 고정폭 눈금 게이지**(10칸)다.
  폭을 가득 채우는 긴 막대는 게임 HUD가 아니라 로딩바처럼 읽힌다
- 상태 판에 **장착한 치장까지 반영된 캐릭터 미니 스프라이트**를 넣는다.
  이미 있는 데이터라 공짜이고, 상태창다워진다
- `나가기`는 캐릭터 화면으로 내렸다. 자주 쓰는 버튼이 아니다

- 셸은 `h-dvh flex overflow-hidden`. **문서 전체를 스크롤시키면
  모바일 주소창이 뜨고 내릴 때마다 하단 탭이 흔들린다**
- 길찾기는 `fixed`가 아니라 **셸의 한 행/열**이다
- **좁은 화면 = 하단 탭** (엄지 도달), **넓은 화면 = 왼쪽 사이드바**
  (가로 여백이 남는데 아래에 깔면 화면 끝까지 눈이 갔다 와야 한다)
- 하단 탭에는 `env(safe-area-inset-bottom)` — 아이폰 홈 인디케이터에 가리면 안 된다
- **사이드바는 화면 전체 높이**를 차지하고 브랜드가 그 머리에 붙는다.
  헤더가 위를 가로지르면 둘이 T자로 부딪혀 어설퍼 보인다
- **사이드바 로고 아래에 밑줄을 긋지 않는다.** 선이 없어야 사이드바가
  **하나의 기둥**으로 읽힌다
- 상태 판·하루 마감·토스트·하이드레이션 게이트는 **공통 레이아웃**에 둔다.
  페이지마다 게이트를 반복하면 탭을 옮길 때마다 로딩이 깜빡인다
- **연속 기록(🔥N)은 상태 판에도 넣는다** — 어느 탭에 있든 보여야 하는 지표다
- 토스트는 하단 탭 위(`bottom-24 sm:bottom-6`)에 띄운다

### `input` / `textarea`
`--surface-sunken` 배경 / `--border-thin` / radius `0` / 포커스 시 보더 `--primary` + `3px`.
`box-shadow` 링(`ring`) 금지 — blur가 들어가는 순간 픽셀이 아니다.

---

## Do / Don't

**Do**
- 액센트는 `--primary` **하나만**. 파스텔 틴트는 데이터(카테고리)를 나타낼 때만.
- 그림자는 언제나 blur `0`, 잉크색, 오프셋 정수 픽셀.
- 아이콘은 이모지 대신 픽셀 스프라이트로 교체해갈 것 (`image-rendering: pixelated`).
- 루틴 앱이므로 **연속성 표시**(스트릭, 잔디, 오늘 완료 수)를 카드보다 위에 둘 것.

**Don't**
- ❌ 그라디언트 (현재 와이어프레임의 `from-purple-500 to-pink-500` — 전부 제거 대상)
- ❌ `rounded-*`, `backdrop-blur`, `opacity` 로 상태 표현
- ❌ 픽셀 폰트로 본문 장문 렌더링
- ❌ 보라/핑크 그라디언트 제목 (`bg-clip-text`) — 브랜드 액센트 1개 원칙 위반

---

## 현재 코드에서 바꿀 것 (`app/page.tsx`, `app/globals.css`)

1. `globals.css`의 `--background: #ffffff` → `--canvas: #F4EEE0`, 위 토큰 전체를 `@theme`에 등록
2. 기존 `.pixel-*` 클래스가 `Courier New`를 쓰고 있음 → **Galmuri11로 교체** (Courier는 픽셀 폰트가 아니라 등폭 폰트다)
3. `app/page.tsx` 전역: `rounded-2xl`/`rounded-xl` → `rounded-none`, `shadow-lg` → `shadow-[4px_4px_0_0_#2E2A24]`, `bg-gradient-to-*` 전부 단색으로
4. `layout.tsx`의 Geist → Galmuri11 + Pretendard
5. 카테고리/랭크 칩 색을 위 매핑 테이블로 고정 (현재 yellow/cyan/green/amber가 의미 없이 흩어져 있음)
