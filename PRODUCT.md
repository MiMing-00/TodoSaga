# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

지금은 **개발자 본인 한 명**을 위한 개인 프로젝트다. 인증이 `ALLOWED_ID` 환경변수 단일 값 + 쿠키뿐이라 사실상 1인 전용 게이트이며, 실제 다중 사용자 인증(회원가입/로그인 체계)은 아직 없다.

다만 완전히 닫혀 있지는 않다 — **추후 공개 가능성을 열어둔 개인 프로젝트**로, 백엔드(Firebase)가 붙는 시점에 다중 사용자·기기 간 동기화로 확장하는 것을 로드맵에 이미 적어두었다(`ROADMAP.md` "백엔드가 생긴 뒤에 할 것"). 언제 공개할지, 누구까지 열지는 아직 결정되지 않았다 — 향후 작업(특히 온보딩, 랜딩/마케팅 표면)은 "지금은 1인용, 열릴 수도 있다"는 이 이중성을 감안해야 한다.

## Product Purpose

**나비사가(TodoSaga)**는 할 일을 RPG 퀘스트로 바꿔주는 개인 루틴/할 일 관리 앱이다. 사용자가 자연어로 할 일을 입력하면, AI 안내자 "나비"(고양이 집사, Gemini로 구현)가 이를 EXP·랭크(F~S)·골드·아이템 보상이 붙은 "퀘스트(의뢰서)"로 바꿔 적는다. 퀘스트를 완수하면 5개 자질(STR/INT/CHA/VIT/LUK)의 경험치가 쌓이고, 자질 하나가 100에 닿으면 직업(전사/현자/음유시인/수도사/방랑자)이 열리며, 연속 기록("불씨")과 재화로 상점에서 장비·도구를 사고 강화하는 순환 경제가 돈다.

성공은 "레벨이 오르는 것"이 아니라 **사용자가 실제로 하루하루 할 일을 미루지 않고 처리하게 되는 것**이다 — 게임화는 수단이지 목적이 아니다.

## Positioning

다른 게이미피케이션 투두 앱과 실제로 갈라지는 지점은 스킨(픽셀·RPG 표현)이 아니라 **도구(道具) 시스템**이다. 상점에서 사는 "도구"는 장식이 아니라 AI 퀘스트 생성 프롬프트에 실제로 주입되는 프롬프트 모디파이어다 — 예: 「첫걸음의 신발」을 장착하면 나비가 만드는 퀘스트에 "처음 5분에 할 일" 필드가 실제로 붙어서 나온다. 즉 게임 재화를 쓸수록 **실제로 미루기를 줄이는 행동 넛지**를 얻는 구조이며, 이게 없으면 이 앱은 그냥 "예쁜 픽셀 투두 앱"으로 전락한다는 것을 팀(=개발자) 스스로 인지하고 있다(`ROADMAP.md`).

두 번째 차별점은 **판타지 레이어가 현실을 가리지 않는다**는 원칙이다. 모든 자질·퀘스트는 세계관 이름(완력/지혜/인망/생명력/여흥) 옆에 항상 현실 분류(운동/공부/사교/건강/취미)를 작게 병기한다 — "무슨 일을 했는지 흐려지면 안 된다"가 `docs/LORE.md`에 명시된 불변 규칙이다.

## Operating Context

- 매일 반복 사용을 전제로 한 개인 루틴 앱. 하루를 열어 나비에게 오늘 할 일을 의뢰하고(자연어 입력 → AI가 퀘스트로 변환), 실제로 그 일을 하고, 완수를 누르고(선택적으로 한 줄 기록을 남기고), 하루가 넘어가면 다음 접속 시 정산 화면에서 그날을 닫는다.
- 브라우저가 자정에 스스로 깨어날 수 없으므로, 날짜 정산은 **다음 앱 접속 시점에** 이루어진다. 며칠 비웠어도 정산 화면은 가장 최근 하루만 보여주고 나머지는 조용히 닫는다.
- 화면은 5개로 분리되어 있다: `/` 퀘스트(오늘의 기록·의뢰 작성·목록), `/character` 캐릭터(전직·연속 기록·능력치), `/bag` 소지품(재화·전리품·장비·조합), `/shop` 상점(구매·판매·뽑기), `/chronicle` 기록(사가의 서 — 월간 기록·책장).
- 전량 한국어 UI. 넓은 화면과 좁은 화면 모두 지원하는 반응형 웹(모바일 웹 포함)이며, 설치형 PWA는 아직 아니다.
- 로컬 환경에서는 개발 서버가 `localhost:3100`에 고정되어 있고(3000·3001은 다른 프로젝트가 씀), 프로덕션에서는 탭 제목/설명이 "나비사가" 브랜드로 바뀐다(개발 중엔 평범한 개발 화면 위장 — 게임 작업 중임을 옆자리에 굳이 드러내지 않으려는 의도).

## Capabilities and Constraints

- **AI 퀘스트 생성**: `@google/genai`(Gemini)로 자연어 입력을 퀘스트 JSON으로 변환한다. `GEMINI_API_KEY`, `QUEST_SYSTEM_PROMPT` 환경변수에 의존. 현재 AI 응답은 `JSON.parse`를 통짜로 try/catch만 하는 상태 — 형식이 깨지면 사용자가 할 수 있는 게 새로고침뿐이다(검증/재시도 로직은 배포 전 체크리스트에만 있고 아직 미구현, `ROADMAP.md`).
- **저장 방식**: 전량 로컬(zustand `persist` → localStorage). 서버 DB가 없다. `firebase` 패키지가 의존성에는 있지만 **코드 어디에도 실제로 연결되어 있지 않다** — 향후 백엔드(다중 사용자 인증·기기 간 동기화·푸시 알림)를 위해 미리 얹어둔 것으로, 착수 시점은 미정이다.
- **인증**: 단일 `ALLOWED_ID` 환경변수 + httpOnly 쿠키. 진짜 회원 시스템이 아니라 "본인만 들어오는 문"이다.
- **알림 없음**: 브라우저 탭이 켜져 있을 때만 뜨는 알림은 의미가 없다고 판단해 구현하지 않았다. 서버+스케줄러가 생기기 전까지는 원천적으로 불가능한 기능으로 로드맵에 명시되어 있다.
- **명명 체계는 여러 곳에 동기화되어 있다**: 카테고리(STR/INT/CHA/VIT/LUK) 라벨은 `QUEST_SYSTEM_PROMPT`가 AI에게 지시하는 분류, `DESIGN.md`의 틴트 매핑 표, `docs/LORE.md`의 용어집이 서로 일치해야 한다. 하나를 고치면 나머지도 같이 고쳐야 하는 제약이 문서에 명시되어 있다.
- **명시적으로 하지 않기로 한 것** (`ROADMAP.md` "안 할 것"): 현금 결제(골드는 오직 하루를 산 대가로만 얻는다), 소셜 피드/랭킹(비교 요소), 알림 남발(하더라도 하루 2회 상한).
- **다크 모드 없음** — 의도적으로 단일 테마만 선택한 상태(필요해지면 나중에 고려).
- **미확정 사실**: persist 스키마에 마이그레이션 함수가 없어 필드 추가 시 기본값으로 땜질 중 — 실사용자 데이터가 쌓이기 전에 반드시 손봐야 하는 항목으로 로드맵에 남아 있다.

## Brand Commitments

- **제품명**: 사용자에게 보이는 이름은 **나비사가**(태그라인: "말하는 고양이를 집사로 들였더니 미루던 일이 전부 의뢰서가 되었다"). 저장소/패키지명 `todosaga`는 내부 개발명일 뿐 노출되는 브랜드명이 아니다.
- **나비(안내자 캐릭터)는 고양이다.** 나비(butterfly)라는 이름과 달리 날개·비늘·깃털 표현을 쓰지 않는다 — 꼬리·앞발·수염·털로만 묘사한다. 이 규칙은 스프라이트뿐 아니라 카피 전반에 적용된다.
- **세계관 잠금 규칙** (`docs/LORE.md`, 임의로 바꾸면 안 됨):
  - 고양이는 빨강·보라 계열을 흐리게 본다는 설정 때문에 나비가 엮는 책은 항상 갈색 가죽이고, 자질 구분은 색이 아니라 표지에 새긴 금박 룬으로만 한다.
  - "녹"(방치가 쌓는 낡음)과 "재"(무리한 몰아붙임이 남기는 흔적)는 둘 다 하루 단위로 되돌릴 수 없다 — 이 비가역성 자체가 세계관의 핵심 설계 의도이므로, 향후 기능이 이를 손쉽게 되돌릴 수 있게 만들면 안 된다.
  - 나비는 "정중하되 가끔 단호한" 집사 말투를 쓰며 **어떤 짧은 대사에도 반말을 쓰지 않는다.** 사용자를 혼내는 카피는 쓰지 않는다("불씨는 그대로예요, 오늘 다시 채우면 됩니다" 식).
  - 자질 이름 옆에는 항상 현실 분류를 병기한다(완력 ⚔ 운동 등) — 위 Positioning 참고.
- **호칭 규칙**: 사용자는 어떤 직업을 갖고 어떤 칭호(견습/숙련/명예/전설)를 달아도 나비에게 "용사"로만 불린다 — 직업은 바뀌어도 이 호칭은 고정이다(세계관상 의도적 설계, `docs/LORE.md` 참고).

## Evidence on Hand

실사용자 후기·사례·수치 데이터는 없다 — 아직 개발자 본인만 쓰는 사전 출시 단계다. 향후 마케팅/랜딩 성격의 작업에서 후기·고객 사례·벤치마크 수치를 **지어내면 안 된다.**

## Product Principles

1. **판타지가 현실을 가리면 안 된다.** 모든 자질·카테고리·퀘스트는 세계관 이름 옆에 항상 평범한 현실 분류를 병기한다 — 이 원칙이 깨지면 "그냥 못 알아먹는 앱"이 된다는 게 세계관 문서 자체의 판단이다.
2. **보상은 실제로 한 일과 연결돼야 한다.** 완수를 누르기만 하면 보상이 나오는 구조는 이미 한 번 "재미없다"고 자체 진단되어, 완수를 2단계(완수 + 선택적 한 줄 기록)로 바꾸고 정산(녹/재)을 비가역으로 설계했다. 이 방향을 거스르는(예: 무한 되돌리기가 가능한) 기능은 추가하지 않는다.
3. **게이미피케이션은 실제 행동 변화를 만들어야 장식이 아니다.** 도구(道具) 시스템처럼 게임 요소가 실제 AI 산출물(첫걸음 안내 등)에 영향을 주는 방식을 우선한다 — 이게 이 앱이 다른 RPG 투두 앱과 갈라지는 지점이라는 게 팀의 자체 결론이다.
4. **나비는 항상 예의 바르고 혼내지 않는다.** 사용자가 며칠을 놓쳐도 나비의 말투와 태도는 일관되게 정중해야 한다.
5. **조작적 리텐션 장치를 쓰지 않는다.** 현금 결제, 소셜 비교/랭킹, 알림 남발은 명시적으로 금지된 방향이다.
